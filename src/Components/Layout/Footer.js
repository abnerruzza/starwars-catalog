import React, {useState, useEffect} from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <footer>

        </footer>
    );
};

Footer.propTypes = {};

export default Footer;
