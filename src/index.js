import React    from 'react';
import ReactDOM from 'react-dom';
import App      from "./App";

/**
 * Primeiro arquivo que renderiza o primeiro component no site
 */
ReactDOM.render(<App />, document.getElementById('root'));
